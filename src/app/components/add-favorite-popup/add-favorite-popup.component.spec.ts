import { async, ComponentFixture, TestBed } from '@angular/core/testing';
import { IonicModule } from '@ionic/angular';

import { AddFavoritePopupComponent } from './add-favorite-popup.component';

describe('AddFavoritePopupComponent', () => {
  // let component: AddFavoritePopupComponent;
  // let fixture: ComponentFixture<AddFavoritePopupComponent>;

  // beforeEach(async(() => {
  //   TestBed.configureTestingModule({
  //     declarations: [ AddFavoritePopupComponent ],
  //     imports: [IonicModule.forRoot()]
  //   }).compileComponents();

  //   fixture = TestBed.createComponent(AddFavoritePopupComponent);
  //   component = fixture.componentInstance;
  //   fixture.detectChanges();
  // }));

  // it('should create', () => {
  //   expect(component).toBeTruthy();
  // });
});
