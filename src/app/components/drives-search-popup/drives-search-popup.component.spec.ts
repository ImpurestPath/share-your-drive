import { async, ComponentFixture, TestBed } from '@angular/core/testing';
import { IonicModule } from '@ionic/angular';

import { DrivesSearchPopupComponent } from './drives-search-popup.component';

describe('DrivesSearchPopupComponent', () => {
  // let component: DrivesSearchPopupComponent;
  // let fixture: ComponentFixture<DrivesSearchPopupComponent>;

  // beforeEach(async(() => {
  //   TestBed.configureTestingModule({
  //     declarations: [ DrivesSearchPopupComponent ],
  //     imports: [IonicModule.forRoot()]
  //   }).compileComponents();

  //   fixture = TestBed.createComponent(DrivesSearchPopupComponent);
  //   component = fixture.componentInstance;
  //   fixture.detectChanges();
  // }));

  // it('should create', () => {
  //   expect(component).toBeTruthy();
  // });
});
